manifest_tests:
  - name: "naming_convention"
    type: "name_convention"
    description: "All nodes must follow the naming convention."
    pattern: "pascal_case"

  - name: "naming_convention"
    type: "name_convention"
    description: "All nodes must follow the naming convention."
    pattern: "snake_case"
  # - name: "models_must_have_tags"
  #   type: "has_tags"
  #   severity: "error"
  #   required_tags:
  #     - "tag1"
  #   applies_to:
  #     - "models"

catalog_tests:
  - name: "columns_are_all_documented"
    type: "columns_are_all_documented"
    description: "All columns in the catalog must have documentation."
    severity: "warning"
